# Core game library
pip install pygame   # (or the latest 2.x release)

# Optional (for future enhancements / tests)
pip install pillow          # image handling, if you later want sprites
pip install pytest          # for unit‑testing the logic
